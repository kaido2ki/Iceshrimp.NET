@using Iceshrimp.Assets.PhosphorIcons
@inject IStringLocalizer<Localization> Loc;
<div class="nav">
    <NavLink href="/settings/preferences">
        <div class="sidebar-btn">
            <Icon Name="Icons.SlidersHorizontal"/>
            <span class="text">@Loc[L.Preferences]</span>
        </div>
    </NavLink>
    <LabeledRuler Label="@Loc[L.User]"/>
    <NavLink href="/settings/profile">
        <div class="sidebar-btn">
            <Icon Name="Icons.User"/>
            <span class="text">@Loc[L.Settings_Profile]</span>
        </div>
    </NavLink>
    <NavLink href="/settings/account">
        <div class="sidebar-btn">
            <Icon Name="Icons.UserCircleGear"/>
            <span class="text">@Loc[L.Settings_Account]</span>
        </div>
    </NavLink>
    <NavLink href="/settings/filters">
        <div class="sidebar-btn">
            <Icon Name="Icons.SpeakerX"/>
            <span class="text">@Loc[L.Settings_Filters]</span>
        </div>
    </NavLink>
    <NavLink href="/settings/import-export">
        <div class="sidebar-btn">
            <Icon Name="Icons.Package"/>
            <span class="text">@Loc[L.Settings_Data]</span>
        </div>
    </NavLink>
    <NavLink href="/settings/migration">
        <div class="sidebar-btn">
            <Icon Name="Icons.AirplaneTakeoff"/>
            <span class="text">@Loc[L.Settings_Migration]</span>
        </div>
    </NavLink>
    <NavLink href="/settings/sessions">
        <div class="sidebar-btn">
            <Icon Name="Icons.Devices"/>
            <span class="text">@Loc[L.Settings_Sessions]</span>
        </div>
    </NavLink>
    <LabeledRuler Label="@Loc[L.Common_Other]"/>
    <NavLink href="/settings/about">
        <div class="sidebar-btn">
            <Icon Name="Icons.Info"/>
            <span class="text">@Loc[L.Settings_About]</span>
        </div>
    </NavLink>
    <NavLink href="/settings/debug">
        <div class="sidebar-btn">
            <Icon Name="Icons.Bug"/>
            <span class="text">@Loc[L.Settings_Debug]</span>
        </div>
    </NavLink>
    <a href="/scalar" target="_blank">
        <div class="sidebar-btn">
            <Icon Name="Icons.ArrowSquareOut"/>
            <span class="text">@Loc[L.Settings_External_ApiReference]</span>
        </div>
    </a>
</div>

@code {
}