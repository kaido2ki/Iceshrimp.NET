<span class="labeled-ruler">
    <span class="rule"></span>
    <span class="label">@Label</span>
    <span class="rule"></span>
</span>

@code {
    [Parameter, EditorRequired] public required string Label { get; set; }
}