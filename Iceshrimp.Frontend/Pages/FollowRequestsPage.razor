@page "/follow-requests"
@using Iceshrimp.Assets.PhosphorIcons
@using Iceshrimp.Frontend.Components
@using Microsoft.AspNetCore.Authorization
@using Microsoft.AspNetCore.Components.Sections
@attribute [Authorize]
@inject IStringLocalizer<Localization> Loc;

<HeadTitle Text="@Loc[L.FollowRequests]"/>

<SectionContent SectionName="top-bar">
    <Icon Name="Icons.HandWaving"></Icon>
    @Loc[L.FollowRequests]
    <a class="action btn" href="/follow-requests/sent">
        <Icon Name="Icons.ArrowSquareOut"/>
        <span>@Loc[L.FollowRequests_SwitchToOutgoing]</span>
    </a>
</SectionContent>

<FollowRequestList Outgoing="false"/>

@code {
}