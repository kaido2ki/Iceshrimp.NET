@page "/notifications"
@using Iceshrimp.Assets.PhosphorIcons
@using Iceshrimp.Frontend.Components
@using Microsoft.AspNetCore.Authorization
@using Microsoft.AspNetCore.Components.Sections
@attribute [Authorize]

@inject IStringLocalizer<Localization> Loc;

<HeadTitle Text="@Loc[L.Common_Notifications]"/>

<SectionContent SectionName="top-bar">
    <Icon Name="Icons.Bell"></Icon>
    @Loc[L.Common_Notifications]

    <span class="action btn" @onclick="MarkAllAsRead" title="@Loc["Mark all as read"]">
        <Icon Name="Icons.Checks"/>
        <span>@Loc["Mark all as read"]</span>
    </span>
</SectionContent>

<NotificationList @ref="NotificationList"/>

@code {
    private NotificationList NotificationList { get; set; } = null!;

    private async Task MarkAllAsRead()
    {
        await NotificationList.MarkAllAsRead();
    }
}