@page "/{User}"
@using System.Text.RegularExpressions
@using Iceshrimp.Frontend.Components
@using Iceshrimp.Frontend.Core.Miscellaneous
@using Iceshrimp.Frontend.Core.Services
@using Iceshrimp.Shared.Schemas.Web
@using Microsoft.AspNetCore.Authorization
@using Iceshrimp.Assets.PhosphorIcons
@using Microsoft.AspNetCore.Components.Sections
@attribute [Authorize]
@inject ApiService                     Api
@inject IJSRuntime                     Js;
@inject IStringLocalizer<Localization> Loc;
@inject NavigationManager              Nav;

<HeadTitle Text="@(State == State.Loaded ? UserResponse.DisplayName ?? UserResponse.Username : Loc[L.Settings_Profile])"/>
<SectionContent SectionName="top-bar-icon">
    <Icon Name="Icons.User"/>
</SectionContent>
<SectionContent SectionName="top-bar">
    @if (State == State.Loaded)
    {
        <UserDisplayName User="@UserResponse"/>
    }
    else
    {
        @Loc[L.Settings_Profile]
    }
</SectionContent>

@if (State == State.Loaded)
{
    <HeadTitle Text="@(UserResponse.DisplayName ?? UserResponse.Username)"/>

    <div class="container">
        @if (UserResponse.Host != null && Profile?.Url != null)
        {
            <RemoteUserBanner OriginalPageUrl="@Profile.Url"/>
        }

        @if (UserResponse.BannerUrl != null)
        {
            <div class="banner">
                <img class="banner" src="@UserResponse.BannerUrl"
                     alt="@(UserResponse.BannerAlt ?? Loc[L.User_BannerAltFallback, UserResponse.DisplayName ?? UserResponse.Username])"
                     title="@(UserResponse.BannerAlt ?? Loc[L.User_BannerAltFallback, UserResponse.DisplayName ?? UserResponse.Username])"/>
            </div>
        }

        <div class="profile-card">
            <div class="header">
                <div class="subheader">
                    <UserAvatar User="@UserResponse" Size="5em" Title="@true"/>
                    <div class="name-section">
                        <div class="name">
                            <UserDisplayName User="@UserResponse"/>
                        </div>
                        <div class="identifier">
                            @@@UserResponse.Username
                            @if (UserResponse.Host != null)
                            {
                                <span class="host">@@@UserResponse.Host</span>
                            }
                        </div>
                    </div>
                </div>
            </div>
            @if (Profile != null)
            {
                <ProfileInfo Emojis="@UserResponse.Emojis" User="UserResponse" UserProfile="Profile"/>
            }
        </div>
        @if (UserPinned.Count > 0)
        {
            <div class="pins-label">
                <LabeledRuler Label="@Loc[L.User_PinnedNotes]" LeftRule="false"/>
                @if (UserPinned.Count > 4)
                {
                    <button class="button" @onclick="SkipPinnedNotes">@Loc[L.User_PinnedNotesSkip]</button>
                }
            </div>
            <div class="notes">
                @foreach (var note in UserPinned)
                {
                    <TimelineNote Note="note"/>
                }
            </div>
            <span @ref="EndPinnedRef" class="pinned-end">
                <ScrollEnd IntersectionChange="AddPinnedNotes" ManualLoad="AddPinnedNotes" Class="end"/>
            </span>
            <div class="pins-label">
                <LabeledRuler Label="@Loc[L.User_LatestNotes]" LeftRule="false"/>
            </div>
        }
        @if (UserNotes.Count > 0)
        {
            <div class="notes">
                @foreach (var note in UserNotes)
                {
                    <TimelineNote Note="note"/>
                }
            </div>
            <ScrollEnd IntersectionChange="AddNotes" ManualLoad="AddNotes" Class="end"/>
        }
    </div>
}
@if (State == State.Loading)
{
    <HeadTitle Text="@Loc[L.StateButton_Loading]"/>

    <div class="loading"><LoadingSpinner Scale="2" /></div>
}
@if (State == State.Error)
{
    <HeadTitle Text="@Loc[L.StateButton_Error]"/>

    <div>Failure</div>
}

@code {
    [Parameter] public string?              User         { get; set; }
    [Parameter] public string?              Host         { get; set; }
    private            IJSObjectReference   Module       { get; set; } = null!;
    private            ElementReference     EndPinnedRef { get; set; }
    private            UserResponse         UserResponse { get; set; } = null!;
    private            UserProfileResponse? Profile      { get; set; }
    private            string?              PinnedMinId  { get; set; }
    private            string?              MinId        { get; set; }
    private            List<NoteResponse>   UserPinned   { get; set; } = [];
    private            List<NoteResponse>   UserNotes    { get; set; } = [];
    private            State                State        { get; set; }

    private bool _fetchLock;

    private async Task GetNotes(string? minId)
    {
        var pq    = new PaginationQuery { Limit = 10, MaxId = minId };
        var notes = await Api.Users.GetUserNotesAsync(UserResponse.Id, pq);
        if (notes is not null && notes.Count > 0)
        {
            MinId = notes.Last().Id;
            UserNotes.AddRange(notes);
            StateHasChanged();
        }
    }
    
    private async Task GetPinnedNotes(string? minId)
    {
        var pq    = new PaginationQuery { Limit = 10, MaxId = minId };
        var notes = await Api.Users.GetUserPinnedNotesAsync(UserResponse.Id, pq);
        if (notes is not null && notes.Count > 0)
        {
            PinnedMinId = notes.Last().Id;
            UserPinned.AddRange(notes);
            StateHasChanged();
        }
    }

    private async Task AddPinnedNotes()
    {
        if (_fetchLock == false)
        {
            _fetchLock = true;
            await GetPinnedNotes(PinnedMinId);
            _fetchLock = false;
        }
    }

    private async Task AddNotes()
    {
        if (_fetchLock == false)
        {
            _fetchLock = true;
            await GetNotes(MinId);
            _fetchLock = false;
        }
    }

    private async Task SkipPinnedNotes()
    {
        await Module.InvokeVoidAsync("ScrollIntoView", EndPinnedRef);
    }

    private async Task LoadProfile()
    {
        State = State.Loading;
        try
        {
            if (User is null)
            {
                Nav.NotFound();
            }
            else
            {
                var pattern      = "^@([^@]+)@?(.+)?$";
                var matches      = Regex.Match(User, pattern);
                var userResponse = await Api.Users.LookupUserAsync(matches.Groups[1].Value, matches.Groups[2].Value);
                if (userResponse is null)
                {
                    Nav.NotFound();
                }
                else
                {
                    UserResponse = userResponse;
                    Profile      = await Api.Users.GetUserProfileAsync(UserResponse.Id);
                    await GetPinnedNotes(null);
                    await GetNotes(null);
                    State = State.Loaded;
                }
            }
        }
        catch (ApiException)
        {
            State = State.Error;
        }
    }

    protected override async Task OnParametersSetAsync()
    {
        UserPinned  = [];
        UserNotes   = [];
        PinnedMinId = null;
        MinId       = null;
        Profile     = null;
        StateHasChanged();
        await LoadProfile();
    }

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            Module = await Js.InvokeAsync<IJSObjectReference>("import",
                                                              "./Pages/ProfileView.razor.js");
        }
    }
}
